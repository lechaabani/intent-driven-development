# PROJECT: [Project Name]

## ARCHITECTURE
- **Stack**: [React, Node, PostgreSQL, etc.]
- **Structure**: [src/components/, api/routes/, etc.]
- **Patterns**: [REST, GraphQL, microservices, etc.]

## CONVENTIONS
- **Style**: [ESLint config, Prettier settings]
- **Naming**: [camelCase for JS, snake_case for Python, etc.]
- **Tests**: [Jest, pytest, coverage target %]
- **Commits**: [Conventional Commits format]

## CRITICAL RULES

### Files You Cannot Touch
- `auth.js` — authentication system
- `payment.js` — payment processing
- `database/migrations/` — schema changes require approval
- `.env` — secrets

### Performance Requirements
- User interactions: <200ms
- API responses: <500ms
- Page load: <2s

### Security Requirements
- No plain-text passwords
- Validate all user input
- Use parameterized queries only

## PATTERNS

### Form Validation
```javascript
import { validateEmail } from '@/utils/validators';
// Validate on blur, show inline errors
```

### API Calls
```javascript
import api from '@/services/api';
const data = await api.get('/endpoint');
```

## GOTCHAS
- [Browser-specific issue, e.g., Safari caching bug]
- [Database limitation, e.g., TEXT column 1GB limit]
- [Framework quirk, e.g., React StrictMode double-renders]

## RECENT DECISIONS
- YYYY-MM-DD: [Decision made + rationale]
- YYYY-MM-DD: [Another decision]

(Older decisions: see CHANGELOG.md)
